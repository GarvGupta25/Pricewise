import type { FinalResult } from "../types";
export function RecommendationPanel({ result }: { result: FinalResult }) { const style = result.recommendation === "buy_now" ? "bg-emerald-50 border-emerald-200 text-emerald-900" : result.recommendation === "wait" ? "bg-amber-50 border-amber-200 text-amber-900" : "bg-slate-100 border-slate-200 text-slate-800"; const title = result.recommendation === "buy_now" ? "Buy now" : result.recommendation === "wait" ? "Wait" : "More price history needed"; return <section className={`rounded-3xl border p-6 ${style}`}><p className="text-xs font-bold uppercase tracking-[.18em]">Recommendation</p><h2 className="mt-2 text-3xl font-extrabold">{title}</h2><p className="mt-3 max-w-3xl leading-7">{result.reasoning}</p>{result.upcoming_sale && <p className="mt-4 rounded-xl bg-white/70 px-4 py-3 text-sm font-semibold">{result.upcoming_sale.name} starts in {result.upcoming_sale.starts_in_days} days.</p>}</section>; }
