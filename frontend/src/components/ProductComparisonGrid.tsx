import type { ProductResult } from "../types";
import { ProductCard } from "./ProductCard";
export function ProductComparisonGrid({ products, onAddToCart }: { products: ProductResult[]; onAddToCart: (product: ProductResult) => void }) { if (!products.length) return null; return <section><div className="mb-4"><p className="text-xs font-bold uppercase tracking-[.18em] text-cyan-300">Verified retailer results</p><h2 className="mt-1 text-2xl font-bold text-white">Top matches</h2></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{products.map((product) => <ProductCard key={product.id} product={product} onAddToCart={() => onAddToCart(product)} />)}</div></section>; }
