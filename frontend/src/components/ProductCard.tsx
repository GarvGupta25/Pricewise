import type { ProductResult } from "../types";
import { PriceSparkline } from "./PriceSparkline";
const money = (value: number) => `₹${value.toLocaleString("en-IN")}`;
export function ProductCard({ product }: { product: ProductResult }) { const low = product.all_time_low !== null && product.current_price <= product.all_time_low * 1.02; return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">{product.source_site}</p>{low && <span className="rounded-full bg-emerald-100 px-2 py-1 text-xs font-bold text-emerald-700">Near all-time low</span>}</div><h3 className="mt-3 font-bold leading-snug text-ink">{product.title}</h3><p className="mt-4 text-2xl font-extrabold text-ink">{money(product.current_price)}</p>{product.rating != null && <p className="mt-1 text-sm text-slate-500">★ {product.rating}{product.review_count ? ` · ${product.review_count.toLocaleString("en-IN")} reviews` : ""}</p>}<div className="mt-5"><PriceSparkline points={product.price_history} /></div><p className="mt-3 text-sm text-slate-500">{product.price_data_status === "insufficient_data" ? "Not enough history for a trend yet." : `90-day average: ${money(product.avg_90_day ?? product.current_price)}`}</p><a className="mt-4 inline-block text-sm font-bold text-coral hover:underline" href={product.source_url} target="_blank" rel="noreferrer">View on {product.source_site} →</a></article>; }
