import type { ProductResult } from "../types";
import { PriceSparkline } from "./PriceSparkline";
const money = (value: number) => `₹${value.toLocaleString("en-IN")}`;
export function ProductCard({ product, onAddToCart }: { product: ProductResult; onAddToCart: () => void }) { const low = product.all_time_low !== null && product.current_price <= product.all_time_low * 1.02; return <article className="rounded-2xl border border-white/10 bg-white/[.045] p-5 shadow-xl"><div className="flex items-start justify-between gap-3"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">{product.source_site}</p>{low && <span className="rounded-full bg-emerald-400/15 px-2 py-1 text-xs font-bold text-emerald-300">Near all-time low</span>}</div><h3 className="mt-3 font-bold leading-snug text-white">{product.title}</h3><p className="mt-4 text-2xl font-extrabold text-white">{money(product.current_price)}</p>{product.rating != null && <p className="mt-1 text-sm text-slate-400">★ {product.rating}{product.review_count ? ` · ${product.review_count.toLocaleString("en-IN")} reviews` : ""}</p>}<div className="mt-5"><PriceSparkline points={product.price_history} /></div><p className="mt-3 text-sm text-slate-400">{product.price_data_status === "insufficient_data" ? "Building price history." : `90-day average: ${money(product.avg_90_day ?? product.current_price)}`}</p><div className="mt-4 flex gap-3"><button onClick={onAddToCart} className="rounded-xl bg-cyan-300 px-3 py-2 text-sm font-bold text-slate-950">Add to shortlist</button><a className="rounded-xl border border-white/15 px-3 py-2 text-sm font-bold text-slate-200" href={product.source_url} target="_blank" rel="noreferrer">Retailer ↗</a></div></article>; }
