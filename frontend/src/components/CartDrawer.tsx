type CartItem = { id: string; title: string; source_site: string; current_price: number };
export function CartDrawer({ items, onRemove }: { items: CartItem[]; onRemove: (id: string) => void }) { return <aside className="rounded-2xl border border-white/10 bg-white/[.045] p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-cyan-300">Your shortlist</p><h2 className="mt-1 text-xl font-bold text-white">Cart · {items.length}</h2></div><span className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-300/15 text-xl">🛒</span></div>{items.length === 0 ? <p className="mt-5 text-sm leading-6 text-slate-400">Add comparison results here. This is a private shortlist, not a retailer checkout.</p> : <ul className="mt-4 space-y-3">{items.map((item) => <li key={item.id} className="rounded-xl bg-black/20 p-3"><p className="text-sm font-semibold text-white">{item.title}</p><p className="mt-1 text-xs text-slate-400">{item.source_site} · ₹{item.current_price.toLocaleString("en-IN")}</p><button onClick={() => onRemove(item.id)} className="mt-2 text-xs font-bold text-rose-300">Remove</button></li>)}</ul>}</aside>; }
